# E-20260903-1688-oauth-transport-001

## 时间与范围

- 日期：2026-09-03（Asia/Taipei）
- 范围：5043656 日常测试入口、卖家子账号应用授权、AgentLoop OAuth callback 到达性
- 代码候选：服务器当前受控 A1 overlay 1688-a1-v40-82c16d0；本轮没有修改代码、schema 或部署

## Observed

- Cursor 通过 1688 官方原生“日常使用入口 → 去测试 → 测试 → 生成并复制测试链接”打开新入口。
- 卖家测试会话仍为 alitestforisv02:云湃智算；页面不再出现“当前子账号尚未获得授权”，说明主账号授权墙已解除。
- 1688 顶层测试壳可以打开，并创建指向 AgentLoop OAuth callback 的跨域 iframe；未出现最终授权/同意按钮。

## Measured

- 服务器时间：2026-09-03T09:05Z 附近；platform_connections 为 authorized=1、error=1，两条凭证的过期时间均早于当前时间。
- 最新 alibaba_1688.oauth.authorized 审计时间仍为 2026-09-02T12:38:10Z；本轮没有新的授权或刷新成功事件。
- channel_availability_snapshots 与 channel_availability_records 仍为 0 行；未调用 A1 同步。
- 服务器本机访问无参数 callback 得到 HTTP 422，说明服务和公网入口可达；Cursor 侧 iframe 观测为 transferSize=0、status=null，没有形成带 code 的应用请求。
- 本机解析 www.agentloop.top 得到合成地址 198.18.2.141，而服务器解析到公网地址；本机 TLS 连接失败。公网 callback 响应还带有 Nginx 的 X-Frame-Options: DENY。

## Inferred / Unknown

- 到达层阻断（本机 DNS/代理/网络路径）是当前未落库的直接原因；仅取消 frame 头不能解释“服务器完全没有收到带 code 的 GET”。
- X-Frame-Options: DENY 是 1688 跨域 iframe 显示失败的独立兼容问题，可能需要只对 callback location 做最小例外，但在到达层修复前不应单独宣称能完成授权。
- 未知：用户侧网络是否已为 www.agentloop.top 配置直连，及下一次新入口是否会真正到达 callback。

## Gate 与下一步

- Gate：INCOMPLETE。主账号授权墙通过；OAuth 换票落库和有效未过期连接未通过。
- 停止条件已保持：未重放旧 callback、未输出 code/state/token/secret、未调用平台写 API、未执行 A1 同步。
- 下一最小探针：在 Cursor 使用直连 DNS/网络（或用户批准的本机临时 hosts/代理例外）后重新生成一次新入口；只看服务器是否出现新的 oauth.authorized 且 token_expires_at 晚于当前时间。成功后才允许执行第一页 A1 可售量同步。

## Follow-up probe: E-20260903-1688-oauth-transport-002

### 时间与范围

- 时间：2026-09-03 18:32:40–18:35:54（Asia/Taipei，Cursor 只读观察）；服务器复核至 18:39:24（Asia/Taipei）。
- 触发：用户在小火箭新增 DOMAIN,www.agentloop.top,DIRECT 后，重新验证 5043656 的日常测试入口。
- 范围：只验证新入口生成、卖家测试壳、callback 到达和服务器授权状态；没有复用旧回调、没有改代码或部署。

### Observed

- Cursor 通过开发者账号的官方原生 UI 重新生成了全新的日常测试入口，没有手工拼接或编辑入口。
- 卖家会话为 alitestforisv02:云湃智算；当前页面没有“未获授权/请添加日常测试账号”提示，也没有最终授权、同意或确认按钮。
- 入口仍停留在 1688 测试壳；callback 被放入跨域 iframe。Cursor 只能观察到 iframe 存在，不能读取跨域正文。

### Measured

- 本机 dig A www.agentloop.top 仍返回合成地址 198.18.0.73，系统 DNS resolver 仍包含 198.18.0.2；这表明 DIRECT 路由规则没有关闭 fake-IP DNS。
- 本机对 callback 的 TLS 探针仍失败；服务器使用 --noproxy 访问回环服务时，/health 为 HTTP 200、无参数 callback 为 HTTP 422，说明服务器应用与回调路由本身可用。
- Cursor 观察到 iframe transferSize=0、status=null，服务器没有收到本轮 callback。
- 服务器 platform_connections 仍为 authorized=1、error=1，未过期 authorized 为 0；最新 alibaba_1688.oauth.authorized 仍为 2026-09-02T12:38:10Z。
- channel_availability_snapshots 与 channel_availability_records 仍为 0 行；未执行 A1 同步。

### Gate 与下一步

- Gate：INCOMPLETE。主账号授权墙已解除，但浏览器到 callback 的网络到达和换票落库仍未通过。
- 最小下一步：在小火箭中把 www.agentloop.top 加入 fake-IP 排除/真实 DNS 例外，或将增强模式切换为 Redir-Host（名称以客户端实际界面为准）；仅添加 DIRECT 路由不足以改变当前 DNS 结果。完成后再由 Cursor 生成一次全新入口。
- 保持停止条件：不要刷新或重放旧带参数回调，不要输出 code/state/token/secret，不要在授权按钮前继续点击，不要执行同步或任何平台写 API。
