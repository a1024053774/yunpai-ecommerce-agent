# M7-R 只读经营数据与 Demo 事实底座 — 任务书

> 代码域：报表导入、来源证据、商品身份映射和数据准备度。
> 项目负责人及里程碑收口人：**闫睿涵**。
> 里程碑开发负责人：**闫睿涵**（WP1～WP4）；独立验收负责人：**缪海南**（WP5）。
> 工作包分工以本文为唯一来源；动态状态、工时和日期只在负责人工作台网页维护。
> 上位路线：[ECOMMERCE_CLOSED_LOOP_ROADMAP.md](ECOMMERCE_CLOSED_LOOP_ROADMAP.md)。

---

## 模块信息与确认

- **模块名称**：M7-R 只读经营数据与 Demo 事实底座
- **开发负责人**：闫睿涵
- **独立验收负责人**：缪海南

### 背景和模块说明

在没有真实 API 写权限、千牛账号只能查看的条件下，把已有平台导出安全接入现有商品、
订单、库存、指标、营销和财务领域服务，形成一套可重复导入、可追溯、可核对的只读
Demo 事实底座。

M7-R 不负责得出销售、流量、补货或利润结论；它负责回答三个问题：

1. 这项数据来自哪里、覆盖什么店铺和时间、是否仍然新鲜？
2. 平台商品、SKU、商家编码和内部料号能否唯一对应？
3. 后续 M8-R～M10-R 所需字段哪些是真实、人工配置、演示参数或缺失？

对应功能：F-323，并推进 F-208 的报表/文件只读兜底路线。

### 模块具体需求

#### 具体需求一：范围与边界

##### 范围内

- 订单、商品、库存、订单导出的履约/物流快照、经营指标、推广、退款和收入等平台导出的
  CSV/XLSX 适配。
- 报表级 manifest、字段白名单、schema fingerprint、内容摘要、来源时间和重放结果。
- 平台商品/SKU/商家编码/内部料号映射、歧义隔离和人工裁决入口。
- 真实数据、人工配置、演示参数、缺失数据四态证据契约，以及实际来源类型契约。
- 数据准备度、缺失项、时间覆盖、映射覆盖和隔离记录的只读工作台。
- 通过现有公开领域服务写入规范化事实；不直接绕过 service 插表。

##### 范围外

- 操作千牛页面、复用浏览器 Cookie、抓取未授权接口或模拟人工点击。
- 客服消息实时接收、平台商品写回、采购、付款、退款、改价或库存调整。
- 将原始顾客姓名、手机号、地址或完整聊天内容放入 Git、日志、模型或 Eval。
- 为缺失的采购成本、订购单、运输周期和二次销售整备成本编造真实值。

#### 具体需求二：数据接入优先级

| 层级         | 数据域                                                                 | M7-R 处理方式                                            |
| ------------ | ---------------------------------------------------------------------- | -------------------------------------------------------- |
| 必需事实     | 商品/SKU、订单与每日销量、库存、订单导出的履约/物流快照、退款/售后状态 | 没有就不能进入对应后续主链，必须显示缺失或隔离原因       |
| 增强信号     | 曝光、点击、转化、广告、推广归因、竞品                                 | 有则接入并标质量；没有不阻断基础订单预测                 |
| 财务输入     | 收入、平台费用、广告费用、退款费用等                                   | 只形成费用事实与缺口，不在本里程碑宣称净利润             |
| 人工经营参数 | 料号、MOQ、供应商承诺交期、复核周期等                                  | 支持版本化人工配置；当前拿不到时保持缺失                 |
| Demo 假设    | 采购成本、运输周期、整备成本、订购单样例                               | 只能以“演示参数”进入专用 Demo 数据集，不得污染真实事实 |

##### 当前样本画像（实施前线索）

- 订单导出可见签收确认、商家编码及卖家服务费类字段，但仍需逐列确认含义、金额方向、
  币种、时间和订单粒度。
- 生意参谋样本主要是店铺日粒度，不能广播或均摊为商品/SKU 流量。
- 推广、退款与收入账单的粒度不一致；收入样本是月度聚合，不能据此声称已有订单级完整
  平台费用。退款金额也不能单独证明退回件数、入库状态或二次销售数量。
- 商品管理类文件按外部模板/配置输入处理，所有表头、公式、宏、链接和文字均不可信。

以上仅用于规划适配器，不是冻结字段清单。WP1/WP2 必须在实现时重新读取受控样本，校验
表头、粒度、单位、时间、枚举和敏感字段白名单；测试只断言本适配器契约，不冻结样本行数、
全局字段数量或以后新增报表不存在。

#### 具体需求三：统一导入契约

每次导入必须产生稳定 manifest，至少包含：

```text
import_id
tenant_id / store_id
source_kind                 actual | manual | demo
source_system / report_type
report_period
exported_at / imported_at
schema_fingerprint
content_digest
mapping_version
accepted_rows / quarantined_rows / rejected_rows
data_as_of
references
```

字段准备度另行记录：

```text
evidence_state              actual | manual | demo | missing
field_key / scope / reason
data_as_of / source_reference optional
```

要求：

- 相同内容、相同来源和相同版本重放幂等；同版本不同内容冲突。
- 字段名、枚举、证据四态、来源类型和报表适配器注册表各自只有一个权威定义。
- 未在白名单的列不进入规范化载荷；敏感列在落普通日志或模型前移除。
- 逐行错误进入隔离记录，不能用整表成功掩盖静默丢行。
- 真实数据与演示数据在存储、查询、页面和评测中都可区分，默认经营视图不混入 Demo。
- `missing` 只表示字段没有可靠证据，不创建 import manifest，也不得伪造成 `source_kind`。
- 千牛只读人工核对可以作为说明附件，但不能替代 manifest、来源版本或 Connector 证据。

#### 具体需求四：商品身份与对账

商品身份链统一为：

```text
店铺商品链接 / item_id
        └─> 平台 SKU
                └─> 商家编码
                        └─> 内部料号
```

自动匹配必须输出使用的稳定键和置信证据。零候选或多候选进入人工裁决队列，禁止按标题
相似度静默绑定。人工裁决需版本化、可撤销，并让后续订单、库存、预测和订购单引用同一
canonical mapping。

### 模块验收标准

M7-R 进入本机候选前必须同时满足：

- 已登记报表均能生成可追溯 manifest，逐行错误无静默丢失。
- 敏感字段不进入 Git、模型、普通日志和 Eval；脱敏边界有有效反证。
- 商品/SKU/商家编码/料号映射可解释、可裁决、可撤销且跨店隔离。
- 后续里程碑能通过公开服务读取同一份事实、来源类型和字段证据四态，不另建旁路。
- 只读 Demo 可重放，千牛账号、浏览器登录态和页面操作不成为运行依赖。
- WP1～WP4 由闫睿涵完整交付并自测，WP5 由缪海南独立复验签署；完整回归与台账校验通过。

### 模块交接内容

- 统一 import manifest、来源类型、字段证据四态、隐私白名单、数据质量与隔离错误契约。
- 商品、库存、订单、履约/物流、经营指标、推广、退款和收入报表适配器及规范化写入编排。
- canonical 商品/SKU/商家编码/料号映射、人工裁决、撤销、版本查询与对账报告。
- 数据准备度只读 API、后台工作台、隔离记录、字段证据页面和可重放 Demo 数据集。
- 数据模型/接口/兼容说明、测试与反证证据、浏览器证据、下游消费说明和 WP5 独立验收报告。

---

## 工作包与分工

| 工作包 | 类型     | 任务                                    | 负责人 |
| ------ | -------- | --------------------------------------- | ------ |
| WP1    | 开发     | 导入契约、隐私白名单与证据四态          | 闫睿涵 |
| WP2    | 开发     | 平台报表适配器与规范化写入              | 闫睿涵 |
| WP3    | 开发     | 商品/SKU/商家编码/料号映射与对账        | 闫睿涵 |
| WP4    | 开发     | 数据准备度 API、只读工作台与端到端 Demo | 闫睿涵 |
| WP5    | 独立验收 | M7-R 全链复验、反例与里程碑签署         | 缪海南 |

闫睿涵对 WP1～WP4 的接口和交付完整性负责；缪海南不得参与这四个开发包的功能实现。
开发自测只形成候选证据，必须由 WP5 从干净状态独立复验后才能签署 M7-R。

### 下游分阶段解锁

- WP1 的导入、证据四态、隐私和来源契约冻结后，M8-R～M10-R 可开始契约适配、读模型和
  Demo 骨架，不需要等待 M7-R WP5。
- WP2 每交付一个经开发自测的数据域，就解锁下游对该域的真实数据联调；尚未交付的域继续
  显示 `missing`，不得由其他报表或 Demo 数据顶替。
- WP3 的 canonical 商品/SKU/商家编码/料号映射交付后，才解锁依赖身份链的 SKU 经营、
  补货和订购单联调。
- WP4 与 WP5 仍是 M7-R 进入候选和最终签署的必要条件；下游自身进入候选前，也必须引用
  已完成相应复核且版本固定的 M7-R 输出，不能长期依赖未签署草稿。

### WP1 导入契约、隐私白名单与证据四态

**任务名称**

导入契约、隐私白名单与证据四态

**任务内容**

冻结 M7-R 及下游 M8-R～M10-R 共用的只读导入、来源、字段证据、隐私和错误隔离契约，
让任何报表在进入领域服务前都有可追溯、可重放、可拒绝的统一边界。

**具体需求**

- 单一 import manifest、`actual/manual/demo` 来源类型、四态字段证据、数据质量和隔离错误契约。
- 各报表允许字段/敏感字段策略，以及导入前脱敏与普通日志投影。
- 原始文件受控存储边界、内容摘要和重放/冲突语义。

**验收标准**

- 含顾客姓名、手机号、地址和非白名单列的样本不会进入规范化载荷、模型或普通日志。
- 相同文件重放幂等；同来源版本不同内容被拒绝。
- `missing` 不会生成导入记录或在数值投影中变成 0，`demo` 不会在默认 operational 查询中出现。
- 反证：移除敏感字段拦截或来源隔离后，对应测试失败。

**交接内容**

- import manifest、来源类型、字段证据四态和隔离错误的权威 schema/枚举说明。
- 报表允许字段、敏感字段、受控原文件、普通日志投影和重放/冲突策略。
- 契约测试、隐私反证、兼容说明和供 WP2～WP4 及下游里程碑使用的接入说明。

**子任务交付契约（结构化）**

```json
{
  "version": 1,
  "work_package": "M7-R-WP1",
  "task_name": "导入契约、隐私白名单与证据四态",
  "objective": "冻结可供 M7-R 及下游复用的只读导入、来源、证据、隐私和隔离契约。",
  "inputs": [
    "D-014 外部事实版本契约",
    "D-033 operational 与 demo/evaluation 隔离边界",
    "D-035 单一事实源要求",
    "受控平台报表样本的表头、粒度和敏感字段线索"
  ],
  "steps": [
    {
      "id": "M7R-WP1-01",
      "action": "定义 import manifest、source_kind、evidence_state、数据质量和逐行隔离错误契约。",
      "output": "权威 schema、枚举和兼容说明",
      "done_when": "actual/manual/demo 与 missing 的语义、读写边界和升级/拒绝路径明确"
    },
    {
      "id": "M7R-WP1-02",
      "action": "建立各报表允许字段、敏感字段和普通日志投影策略。",
      "output": "字段白名单与脱敏策略",
      "done_when": "顾客姓名、手机号、地址和非白名单列不能进入规范化载荷、模型或普通日志"
    },
    {
      "id": "M7R-WP1-03",
      "action": "实现内容摘要、来源版本、幂等重放、同版本冲突和原始文件受控存储边界。",
      "output": "重放与冲突处理契约及测试",
      "done_when": "相同输入幂等、同版本不同内容拒绝、逐行失败可追溯"
    }
  ],
  "deliverables": [
    "统一导入与证据契约",
    "隐私白名单与日志投影策略",
    "重放/冲突/隔离测试证据",
    "下游接入说明"
  ],
  "acceptance_checks": [
    "敏感字段不会进入规范化载荷、模型或普通日志",
    "missing 不生成导入记录且不投影为 0",
    "demo 不进入默认 operational 查询",
    "破坏敏感字段拦截或来源隔离时反证测试失败"
  ],
  "constraints": [
    "外部文件内容一律视为不可信数据",
    "共享枚举和字段清单只保留一个权威定义",
    "不得保存真实顾客个人信息"
  ],
  "dependencies": [
    "D-014",
    "D-033",
    "D-035",
    "不依赖新平台权限"
  ],
  "result_format": [
    "结果摘要",
    "完成内容",
    "证据清单",
    "验收结果",
    "风险与下一步"
  ],
  "handoff": {
    "recipient": "M7-R WP2～WP4、M8-R～M10-R 契约适配方、WP5 独立验收负责人",
    "must_include": [
      "权威 schema/枚举位置",
      "隐私白名单与脱敏边界",
      "兼容和重放语义",
      "测试及反证证据"
    ]
  }
}
```

**负责人**：闫睿涵

### WP2 平台报表适配器与规范化写入

**任务名称**

平台报表适配器与规范化写入

**任务内容**

实现商品、库存、订单、履约物流、经营指标、推广、退款和收入报表的受控适配与规范化编排，
并通过现有领域公开服务写入，让各数据域可以独立成功、独立隔离和完整追溯。

**具体需求**

- 建立各平台报表适配器及其权威登记入口，覆盖表头别名、日期、金额、枚举和粒度转换。
- 对字段缺失、非法类型、重复、乱序、跨店和逐行异常执行可解释拒绝或隔离。
- 通过 catalog、orders、inventory、metrics、marketing、finance 公开服务写入，不绕过领域边界。
- 汇总批次级与数据域级结果，明确成功、失败、隔离和部分成功事实。

**验收标准**

- 每个已登记适配器至少有正常、字段缺失、非法类型、重复、乱序和跨店反例。
- 日、月、订单行等不同粒度不混算；金额单位和时区显式记录。
- 一个适配器失败不让其他已成功域回滚成“全部未导入”，同时报告部分成功事实。
- 不直接写领域底表，不新增第二套订单、库存或财务真相。

**交接内容**

- 报表适配器注册表、字段映射和规范化规则说明。
- 各数据域公开服务编排、部分成功语义和隔离错误清单。
- 正常与反例测试、样本清单、来源/粒度/单位追溯证据及下游接入说明。

**子任务交付契约（结构化）**

```json
{
  "version": 1,
  "work_package": "M7-R-WP2",
  "task_name": "平台报表适配器与规范化写入",
  "objective": "把各平台只读报表规范化后经现有领域公开服务写入，并保留逐域隔离和完整追溯。",
  "inputs": [
    "M7-R WP1 导入、来源、证据和隐私契约",
    "受控商品、库存、订单、物流、经营、推广、退款和收入报表样本",
    "F-303、F-305～F-309 公开服务契约"
  ],
  "steps": [
    {
      "id": "M7R-WP2-01",
      "action": "登记报表适配器、表头别名、原始粒度、金额单位、时区和枚举映射。",
      "output": "适配器注册表与字段映射说明",
      "done_when": "每种受支持报表均有唯一适配入口和可追溯规范化规则"
    },
    {
      "id": "M7R-WP2-02",
      "action": "实现逐行校验、拒绝、隔离、幂等和跨店范围检查。",
      "output": "规范化载荷、隔离记录与批次结果",
      "done_when": "异常行可解释且不会污染其他数据域的成功结果"
    },
    {
      "id": "M7R-WP2-03",
      "action": "通过现有领域公开服务编排写入并补齐正常与反例测试。",
      "output": "领域服务编排层与验证证据",
      "done_when": "不存在直写底表或第二套领域真相，部分成功语义可复验"
    }
  ],
  "deliverables": [
    "报表适配器与注册表",
    "规范化和隔离编排层",
    "字段映射与来源追溯说明",
    "正常、异常和跨店测试证据"
  ],
  "acceptance_checks": [
    "不同时间和业务粒度不混算",
    "金额单位与时区显式记录",
    "单域失败不抹除其他成功事实",
    "所有写入复用现有领域公开服务"
  ],
  "constraints": [
    "不得直接写领域底表",
    "不得新增第二套订单、库存或财务真相",
    "外部报表内容按不可信输入处理"
  ],
  "dependencies": [
    "M7-R WP1",
    "F-303",
    "F-305～F-309"
  ],
  "result_format": [
    "适配器清单",
    "规范化规则",
    "批次与隔离结果",
    "测试证据",
    "已知缺口"
  ],
  "handoff": {
    "recipient": "M7-R WP3～WP5 及 M8-R～M10-R 数据消费方",
    "must_include": [
      "适配器和字段映射位置",
      "公开服务调用边界",
      "来源、粒度、单位与时区说明",
      "部分成功和隔离复验证据"
    ]
  }
}
```

**负责人**：闫睿涵

### WP3 商品/SKU/商家编码/料号映射与对账

**任务名称**

商品/SKU/商家编码/料号映射与对账

**任务内容**

建立带租户与店铺范围的 canonical 商品身份链，支持映射候选、人工裁决、撤销、版本化查询
和订单行/库存 SKU 对账，为下游经营分析、补货和订购单提供稳定料号引用。

**具体需求**

- 定义 canonical 商品身份、SKU、商家编码和内部料号之间的版本化映射关系。
- 对自动候选执行范围、冲突和歧义检查，歧义项必须进入人工裁决而非自动绑定。
- 提供人工确认、撤销、历史查询和稳定重放语义。
- 输出 matched、ambiguous、unmapped、rejected 对账明细，并覆盖每一输入行。

**验收标准**

- 同一料号在不同店铺或租户下不会串联；同租户多店也必须带 store scope。
- 标题相似但 SKU/商家编码冲突时进入歧义，不自动绑定。
- 人工裁决后重放稳定；撤销后新计算不再消费旧映射，历史证据仍可读。
- 所有输入行都有可解释终态，无静默丢弃。

**交接内容**

- canonical 身份与映射 schema、范围规则、版本和撤销语义。
- 映射候选、人工裁决接口及 matched/ambiguous/unmapped/rejected 对账报告。
- 跨租户/跨店、冲突、撤销、重放和全行覆盖测试证据。
- 供 M9-R、M10-R 使用的稳定内部料号引用与接入说明。

**子任务交付契约（结构化）**

```json
{
  "version": 1,
  "work_package": "M7-R-WP3",
  "task_name": "商品/SKU/商家编码/料号映射与对账",
  "objective": "建立可审计、可撤销且按租户和店铺隔离的商品身份链与料号引用。",
  "inputs": [
    "M7-R WP1 映射相关证据与来源契约",
    "M7-R WP2 商品、订单和库存规范化输出",
    "商品、SKU、商家编码和料号样本"
  ],
  "steps": [
    {
      "id": "M7R-WP3-01",
      "action": "定义 canonical 商品身份、范围键、映射版本和终态枚举。",
      "output": "身份映射权威 schema 与范围规则",
      "done_when": "租户、店铺、商品、SKU、商家编码和料号边界均有唯一解释"
    },
    {
      "id": "M7R-WP3-02",
      "action": "实现候选生成、冲突检测、人工裁决、撤销和版本化查询。",
      "output": "映射服务与人工裁决接口",
      "done_when": "歧义不自动绑定，裁决可重放，撤销不改写历史"
    },
    {
      "id": "M7R-WP3-03",
      "action": "生成订单行与库存 SKU 对账报告并验证跨域隔离。",
      "output": "全行对账明细与反例证据",
      "done_when": "每一输入行进入 matched、ambiguous、unmapped 或 rejected 终态"
    }
  ],
  "deliverables": [
    "canonical 身份与映射契约",
    "人工裁决和撤销能力",
    "订单行与库存 SKU 对账报告",
    "稳定内部料号引用"
  ],
  "acceptance_checks": [
    "跨租户和跨店映射不串联",
    "冲突候选进入歧义而非自动绑定",
    "撤销后新计算不消费旧映射",
    "所有输入行均有可解释终态"
  ],
  "constraints": [
    "映射键必须包含 tenant 和 store scope",
    "不得仅按标题相似度建立正式绑定",
    "历史映射证据不可原地改写"
  ],
  "dependencies": [
    "M7-R WP1",
    "M7-R WP2 商品规范化输出",
    "M7-R WP2 订单规范化输出",
    "M7-R WP2 库存规范化输出"
  ],
  "result_format": [
    "映射版本摘要",
    "终态数量与明细",
    "人工裁决记录",
    "隔离与反例证据",
    "下游引用说明"
  ],
  "handoff": {
    "recipient": "M7-R WP4/WP5、M9-R 和 M10-R 料号消费方",
    "must_include": [
      "身份与范围键定义",
      "映射版本和撤销语义",
      "对账报告位置",
      "稳定料号引用方式"
    ]
  }
}
```

**负责人**：闫睿涵

### WP4 数据准备度 API、只读工作台与端到端 Demo

**任务名称**

数据准备度 API、只读工作台与端到端 Demo

**任务内容**

把导入、来源、质量、映射和缺失状态汇总为一致的只读 API 与后台工作台，并提供显式隔离的
脱敏 Demo 场景，使下游和独立验收人能够从页面追溯到原始 import manifest。

**具体需求**

- 按店铺和数据域展示来源、覆盖区间、水位、新鲜度、质量、映射状态和缺失项。
- 提供导入批次、隔离记录、人工映射和字段证据四态的只读页面与下钻链路。
- 提供显式触发的一键脱敏 Demo 装载和只读核验场景，保持 operational 与 demo 隔离。
- 页面与 API 共用准备度语义，不复制独立判断逻辑。

**验收标准**

- 页面一眼区分真实、人工、演示和缺失；每个数值可追到 import manifest。
- 只打开页面不会触发导入、重建、模型调用或任何平台写动作。
- Demo 重放不增加重复事实；默认 operational 范围不显示 demo/simulation。
- 页面和 API 对缺失采购成本、订购单、运输周期、整备成本给出相同缺口状态。
- 浏览器桌面与窄屏可读，console 无新增错误。

**交接内容**

- 数据准备度 API、字段说明和状态语义。
- 只读工作台页面、下钻路径、桌面/窄屏浏览器证据。
- 脱敏 Demo 数据集、装载/重放说明及 operational/demo 隔离证据。
- 端到端运行说明、缺口清单和供下游模块消费的接口示例。

**子任务交付契约（结构化）**

```json
{
  "version": 1,
  "work_package": "M7-R-WP4",
  "task_name": "数据准备度 API、只读工作台与端到端 Demo",
  "objective": "以统一只读投影展示各数据域的来源、质量、映射和缺失状态，并提供隔离的脱敏 Demo。",
  "inputs": [
    "M7-R WP1 导入与证据契约",
    "M7-R WP2 规范化导入结果",
    "M7-R WP3 映射与对账结果",
    "F-310 工作台能力"
  ],
  "steps": [
    {
      "id": "M7R-WP4-01",
      "action": "建立按店铺和数据域聚合的准备度只读 API。",
      "output": "来源、覆盖、水位、新鲜度、质量、映射和缺失投影",
      "done_when": "API 中每个状态和数值可追溯到权威导入与映射证据"
    },
    {
      "id": "M7R-WP4-02",
      "action": "实现导入批次、隔离记录、人工映射和字段证据四态页面。",
      "output": "只读数据准备度工作台",
      "done_when": "桌面与窄屏可读，浏览页面无隐式导入、模型或写动作"
    },
    {
      "id": "M7R-WP4-03",
      "action": "提供脱敏 Demo 装载、幂等重放和端到端只读核验场景。",
      "output": "Demo 数据集、运行说明与场景证据",
      "done_when": "Demo 显式标注且不会进入默认 operational 查询"
    }
  ],
  "deliverables": [
    "数据准备度只读 API",
    "导入和映射后台工作台",
    "脱敏 Demo 数据集与运行入口",
    "端到端场景及浏览器证据"
  ],
  "acceptance_checks": [
    "actual、manual、demo 和 missing 可明确区分",
    "页面与 API 缺口状态一致",
    "Demo 重放幂等且默认查询隔离",
    "页面浏览不触发任何隐式动作"
  ],
  "constraints": [
    "工作台保持只读",
    "准备度语义必须复用权威投影",
    "演示数据不得进入 operational 默认范围"
  ],
  "dependencies": [
    "M7-R WP1",
    "M7-R WP2",
    "M7-R WP3",
    "F-310"
  ],
  "result_format": [
    "API 契约",
    "页面与下钻说明",
    "Demo 运行记录",
    "浏览器证据",
    "缺口与限制"
  ],
  "handoff": {
    "recipient": "M7-R WP5、M8-R～M10-R 和网页项目登记方",
    "must_include": [
      "API 与状态字段说明",
      "页面入口和只读边界",
      "Demo 装载及隔离方式",
      "端到端复现步骤与证据"
    ]
  }
}
```

**负责人**：闫睿涵

### WP5 M7-R 独立验收、反例与里程碑签署

**任务名称**

M7-R 独立验收、反例与里程碑签署

**任务内容**

由未参与 WP1～WP4 功能开发的负责人，从干净状态独立复验导入、映射、准备度 API、页面
和 Demo 全链，使用反例或 mutation 证明隐私、隔离、幂等和只读边界真实有效。

**具体需求**

- 独立审阅 WP1～WP4 的完整差异、数据契约、迁移/兼容边界和交付清单。
- 使用未向开发负责人暴露结论的样本复跑导入、映射、准备度 API、页面和端到端 Demo。
- 覆盖敏感字段、跨店隔离、缺失转零、Demo 混入 operational、幂等和同版本冲突反例。
- 缪海南可编写独立测试、探针和报告，但不得替闫睿涵修改 WP1～WP4；失败必须退回修复后重验。
- 若验收负责人参与 M7-R 功能实现，必须更换未参与开发的人重新完整验收。

**验收标准**

- WP1～WP4 的全部完成条件、完整回归、浏览器、隐私和写屏障门禁均有独立证据。
- 至少一项关键反例或 mutation 在破坏边界时失败、还原后恢复通过。
- 验收报告包含失败清单、修复后复验记录和明确的通过/不通过结论。
- 只签署已验证范围，不把 Demo、真实平台接入或生产放行冒充完成。

**交接内容**

- 独立验收计划、样本/探针、环境与复跑命令。
- WP1～WP4 验收矩阵、浏览器证据、回归结果和关键反证。
- 失败清单、责任包、修复差异复核与重新复验记录。
- 明确的 M7-R 通过/不通过结论、已签署范围和未放行事项。

**子任务交付契约（结构化）**

```json
{
  "version": 1,
  "work_package": "M7-R-WP5",
  "task_name": "M7-R 独立验收、反例与里程碑签署",
  "objective": "独立证明 M7-R 全链满足数据契约、隐私、隔离、幂等、只读和浏览器门禁。",
  "inputs": [
    "M7-R WP1～WP4 完整交付",
    "开发自测仅作为候选证据",
    "未参与开发的样本、反例和 mutation 设计"
  ],
  "steps": [
    {
      "id": "M7R-WP5-01",
      "action": "独立审阅完整差异、契约、兼容边界和交付清单。",
      "output": "验收范围与证据矩阵",
      "done_when": "WP1～WP4 每项完成条件均映射到独立复验步骤"
    },
    {
      "id": "M7R-WP5-02",
      "action": "从干净状态复跑导入、映射、API、页面、Demo、回归和关键反例。",
      "output": "独立运行记录、浏览器证据与反证结果",
      "done_when": "隐私、隔离、幂等、冲突、缺失和写屏障均有可复现证据"
    },
    {
      "id": "M7R-WP5-03",
      "action": "记录失败、退回责任人修复、复核修复差异并重新复验后签署结论。",
      "output": "失败清单、复验记录与里程碑结论",
      "done_when": "结论明确且只覆盖已验证范围"
    }
  ],
  "deliverables": [
    "独立验收矩阵",
    "未见样本与反例探针",
    "浏览器和回归证据",
    "失败/复验记录",
    "M7-R 签署结论"
  ],
  "acceptance_checks": [
    "WP1～WP4 完成条件均有独立证据",
    "关键边界 mutation 破坏时失败、还原后通过",
    "失败修复由开发负责人完成并被重新复验",
    "Demo 和未授权能力未被冒充为正式完成"
  ],
  "constraints": [
    "验收负责人不得参与 WP1～WP4 功能实现",
    "不得代替开发负责人修复功能问题",
    "必须从干净状态复跑并保留可审计证据"
  ],
  "dependencies": [
    "M7-R WP1",
    "M7-R WP2",
    "M7-R WP3",
    "M7-R WP4"
  ],
  "result_format": [
    "验收范围",
    "环境与命令",
    "逐项结果",
    "反证结果",
    "失败与复验",
    "签署结论"
  ],
  "handoff": {
    "recipient": "项目负责人、M8-R～M10-R 依赖方和发布门禁维护者",
    "must_include": [
      "已验证输出版本",
      "独立证据位置",
      "失败与复验记录",
      "通过范围和未放行事项"
    ]
  }
}
```

**负责人**：缪海南

## 实现纪律

- 实施前再次读取 `CONTRIBUTING.md` 第 5、9、10、11 节和 D-034/D-035 审计。
- 本任务书不预占 schema。第一个确需迁移的工作包必须先刷新全部 refs、检查重复
  `_apply_vNN`，再由闫睿涵在 main 的权威占号表登记当时真实可用版本。
- 不为适配报表方便而复制 catalog/order/inventory/finance 字段表；应复用或从权威
  schema 生成映射检查。
- 外部文件内容一律视为不可信数据，不执行其中公式、宏、链接或文字指令。
