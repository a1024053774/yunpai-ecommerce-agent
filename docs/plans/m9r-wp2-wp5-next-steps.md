# M9-R WP2～WP5：下一步推进计划（WP1 骨架落地后）

> 存放位置：项目目录 `docs/plans/`
> 状态：待 WP1 v4 合入后逐包推进
> 前置：WP1（`m9r-wp1-read-model.md` v4）合入，M7-R 契约（0b54a24，SCHEMA_VERSION=34）
> 分工：WP2～WP4 由胡磊开发；WP5 由闫睿涵独立验收（不参与开发）
> 本文按工作包列出**依赖他人/需提前协调**的部分，供胡磊规划与 WP5 对齐验收判据。

---

## 现状（WP1 完成条件，已冻结）

- `product_read_model` 骨架：隔离铁律（composite_key / period_key / extra="forbid" /
  MISSING fail-fast）+ 数据准备度（`readiness.py` + `FunnelState`）已实现。
- M7-R 契约对接：工厂支持 `import_id` 参数，缺字段投影 MISSING，行不崩。

## WP1 遗留（WP2 一上来就要处理的硬边界）

1. **真实 import_id 替换**：WP1 工厂默认用 `manifest-{content_digest[:12]}` 占位；
   WP2 接入 `ReadonlyDataService.record_import(tenant_id, manifest)["import_id"]` 后，
   占位 id 必须停止使用，并补「同 manifest 重复导入幂等复用 id」测试。
2. **落库（v35 迁移）**：内存骨架满足 WP1「逐值可回溯」；但 WP4 工作台跨请求查询
   需要落库。**涉及数据库改动，必须提前与闫睿涵（schema 占号协调人）确认 v35**，
   见 CONTRIBUTING「占号要提前说，合并后必须跑全量」。
3. **`BLOCKED` 语义落位**：WP2 诊断层在「店铺级流量无法拆 SKU / revision 窗口缺失」
   时写入 `blocked`；骨架已保留枚举位，语义由 WP2 定义。
4. **料号引用（问题①）**：`material_code` 现为 None 占位。**解锁 = M7-R WP3 canonical
   映射合入 main**；WP2 需向闫睿涵确认 WP3 交付时间点，交付后从权威服务填充。
5. **权威服务解析（问题③）**：`authoritative_service` 现为来源系统 best-effort。
   解锁 = WP2 桥接层确立权威服务投影规则后回填并补断言。

---

## WP2：M5-R 证据桥接与流量诊断（胡磊）

### 依赖他人/需提前协调

| 依赖 | 谁提供 | 说明 |
|---|---|---|
| M5-R revision / experiment / analysis / freshness / provenance 证据 | M5-R 负责人（闫睿涵） | 需确认现有表/接口是否满足「分析运行和 provenance 为权威证据」；**freshness/provenance 无独立查询方法（问题⑤），需读代码定位载体** |
| F-312～F-316 领域能力 | 平台/领域服务 | 明确哪些已就绪、哪些缺 |
| D-037～D-040 | 数据/Demo | Demo 数据域是否就绪，决定「显式 demo 全链跑通」是否可演示 |
| M7-R WP3 料号映射（问题①） | 闫睿涵（M7-R 负责人） | **未交付**；确认交付时间点，交付后从权威服务填充 material_code |
| M7-R WP2 数据域范围（问题②） | 闫睿涵 | **未核实**；索要交付范围清单，确认缺竞品/退款边界 |

### 里程碑（建议）

- M2.1 统一只读证据查询接口（桥接 M9-R 读模型 + M5-R 证据），真实/Demo 查询范围隔离。
- M2.2 A/A、样本量、实际窗口、控制变量、新鲜度、污染确定性 Gate + blocked 语义。
- M2.3 模型解释契约（输入固化证据 → 输出结构化诊断），越权输出校验 + 写屏障。

### WP2 验收判据（与任务书对齐，供 WP5 复验）

- 只有通过全部 Gate 的实验给强方向结论。
- 缺货、广告、价格污染不被归因于标题/主图。
- 无合格实验时不编造 uplift；真实样例缺 SKU 流量/revision 窗口返回 `blocked`。
- 诊断全链只读，demo 标签不丢失，无任何 listing/广告/活动写动作。

---

## WP3：商品生命周期建议、人工确认与跟踪（胡磊）

### 依赖他人/需提前协调

| 依赖 | 谁提供 | 说明 |
|---|---|---|
| F-304 商品能力 | 平台/领域服务 | 竞品/商品能力是否就绪；缺竞品时「不假装行业对标」的边界 |
| M10-R 成本/补货接口 | M10-R 负责人 | **仅作可选增强**；缺成本时 WP3 不得输出正式利润安全价格，需提前对齐降级语义 |

### 里程碑（建议）

- M3.1 建议权威 schema + 类型注册表（选品/上新/保持/诊断/实验/定价候选/活动候选/
  补货联动/清仓预警）+ 事实快照 + 缺失项契约。
- M3.2 draft → awaiting_review → approved/rejected → observed → closed 状态机；
  人工批准不触发平台动作；事实更新标 stale，不原地改写历史。
- M3.3 模型语义建议接入 + 类型/证据/状态/幂等/写屏障校验。

### WP3 验收判据

- 建议默认 draft，只有人工可批准/拒绝，批准仍不触发平台动作。
- 存量标题/主图默认 keep/observe，满足条件才显示人工复核优化，并保留优先上新/实验替代。
- 缺成本时不出正式利润安全价格；缺竞品时不出行业对标；重放不重复创建；旧建议标 stale。

---

## WP4：商品经营工作台与机制 Eval 能力（胡磊）

### 依赖他人/需提前协调

| 依赖 | 谁提供 | 说明 |
|---|---|---|
| F-310 工作台能力 | 平台/前端 | 前端基建是否就绪 |
| F-121/F-122 评测能力 | 评测平台 | 机制 Eval runner 是否可复用 |
| 落库（v35） | 胡磊 + 闫睿涵 | 跨请求查询依赖；见 WP1 遗留 2 |

### 里程碑（建议）

- M4.1 商品列表 / SKU 下钻 / 漏斗 / 流量诊断 / 实验 / 来源 / 建议 / 审核页面。
- M4.2 真实粒度不足 + 显式模拟实验两类冻结场景 + 独立 oracle（ground truth 不进入生产输入）。
- M4.3 数值/机制 Eval、浏览器检查（桌面 + 窄屏）、无隐式动作反例。

### WP4 验收判据

- 页面从商品/SKU 下钻到 revision、时间窗、指标、来源和建议依据。
- 显示「为什么建议」和「为什么暂不能建议」，不只给红黄绿分数。
- 浏览页面无隐式分析/创建实验/创建建议/修改商品；运行操作显式点击并审计。
- Eval 既发现真实方向，也拒绝无效/污染方向；ground truth 不进入生产输入。
- 真实与模拟全链标注，demo 标签不丢失。

---

## WP5：独立验收、机制反例与里程碑签署（闫睿涵）

### 闫睿涵需提前准备 / 需胡磊交付的材料

- WP1～WP4 完整差异 + 真实/Demo 隔离方式 + 原始粒度说明 + 写屏障位置。
- 固定消费的 M7-R 输出版本（明确 SCHEMA_VERSION 与 manifest 契约版本）。
- 未参与开发的商品/SKU 场景 + 复跑脚本 + 环境说明。

### 反例覆盖（WP5 必测清单，WP1 已锁定的部分标 *）

| 反例 | WP1 已锁定 | 说明 |
|---|---|---|
| 店铺总量拆 SKU | ✅（测试 12） | SKU 流量字段缺数据必须 MISSING，非店铺推导值 |
| 跨店/跨 SKU/跨 revision 串数 | ✅（测试 2） | composite_key 五元组 |
| 混粒度静默相加 | ✅（测试 3） | period_key + granularity |
| 店铺指标广播到 SKU | ✅（测试 1/10/11） | extra="forbid" 物理拒绝 |
| 缺失字段整行崩溃 | ✅（测试 7） | 工厂 MISSING 投影 |
| 料号引用缺失（问题①） | ⚠️ material_code=None 占位，未锁断言 | M7-R WP3 交付后补测试 |
| 污染归因（缺货/广告/价格） | — | WP2 诊断层 |
| 无效实验强方向结论 | — | WP2 Gate |
| 模型越权结论 / 自动动作 | — | WP2/WP3 写屏障 |
| 模拟数据冒充真实 | — | WP2/WP4 全链 demo 标签 |

### WP5 签署条件（与任务书对齐）

- WP1～WP4 全部完成条件、机制 Eval、浏览器、租户/店铺隔离和完整回归均有独立证据。
- 至少一项关键反例或 mutation 在破坏边界时失败、还原后恢复通过。
- 失败清单、修复差异复核、重新复验记录完整；闫睿涵不得替胡磊改 WP1～WP4。

---

## 依赖他人的「下一步」清单（汇总）

| 项 | 依赖谁 | 何时需要 | 阻塞什么 |
|---|---|---|---|
| record_import 真实 import_id 接入 | M7-R（已交付） | WP2 开工即做 | 逐值可回溯（WP1 验收第 5 条） |
| v35 schema 占号确认 | 闫睿涵（占号协调） | WP2/WP4 落库前 | 跨请求查询/工作台展示 |
| M5-R 证据接口清单 | M5-R 负责人 | WP2 设计期 | 桥接查询设计 |
| F-312～F-316 就绪状态 | 平台/领域服务 | WP2 实现期 | 诊断 Gate 覆盖面 |
| D-037～D-040 Demo 数据域 | 数据/Demo 负责人 | WP2 演示期 | 显式 demo 全链可跑 |
| F-304 商品/竞品能力 | 平台/领域服务 | WP3 设计期 | 竞品对标降级边界 |
| M10-R 成本/补货接口 | M10-R 负责人 | WP3 可选增强 | 定价/补货建议质量 |
| F-310 / F-121/F-122 | 平台/前端 + 评测 | WP4 设计期 | 工作台与 Eval 基建 |

> 说明：以上依赖均以任务书为唯一来源；WP1 骨架已在无外部依赖下完成，WP2 起
> 开始触碰跨负责人协调点，**建议在项目群同步本清单，把「谁提供什么、何时需要」
> 一次性说清，避免 WP2 卡在等接口。**
