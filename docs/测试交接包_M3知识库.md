# M3 知识库与 RAG 检索链路 — 测试交接包

> 给测试人（M4/M5 负责人）的独立测试指南。拿到本目录后，按此文档可独立完成全部测试。
> 交接人：胡磊 · 2026-08-05

---

## 一、你要测什么（验收标准）

| 交付物 | 验收标准 |
|---|---|
| 知识图谱（Neo4j） | 部署完成、数据导入、可查核心实体/关系 |
| 图谱检索 API | 独立 API 可被调用，返回结构化结果 |
| 四套 Prompt | 检索结果注入 + 防幻觉 |
| 检索质量评测 | 30+ 问题测试集通过 |

---

## 二、环境准备（约 30 分钟）

### 1. Python 环境（项目已有）

```bash
python3.11 -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
python -m pip install -e ".[dev]"
```

### 2. Neo4j（需安装 + 启动）

```bash
# 本机已装：D:\neo4j-community-2026.04.0
# 启动（需要 Java 17+）
D:\neo4j-community-2026.04.0\bin\neo4j.bat console
# 账号：neo4j / ${NEO4J_PASSWORD:-change-me}
```

### 3. 重建知识图谱数据（关键！Neo4j 数据不在 git，需重建）

```bash
# 拷贝 CSV 到 Neo4j 导入目录
mkdir -p D:\neo4j-community-2026.04.0\import\kg
cp knowledge_graph_output/04_import/nodes_*.csv D:\neo4j-community-2026.04.0\import\kg\
cp knowledge_graph_output/04_import/rels_*.csv D:\neo4j-community-2026.04.0\import\kg\

# 依次执行 3 个 Cypher（约束→节点→关系）
D:\neo4j-community-2026.04.0\bin\cypher-shell.bat -a bolt://localhost:7687 -u neo4j -p ${NEO4J_PASSWORD:-change-me} \
  --file knowledge_graph_output/04_import/00_setup.cypher
D:\neo4j-community-2026.04.0\bin\cypher-shell.bat -a bolt://localhost:7687 -u neo4j -p ${NEO4J_PASSWORD:-change-me} \
  --file knowledge_graph_output/04_import/01_load_nodes.cypher
D:\neo4j-community-2026.04.0\bin\cypher-shell.bat -a bolt://localhost:7687 -u neo4j -p ${NEO4J_PASSWORD:-change-me} \
  --file knowledge_graph_output/04_import/02_load_rels.cypher

# 验证数据
MATCH (n) RETURN labels(n)[0], count(*) ORDER BY count(*) DESC
# 期望: FAQ 60+ / Script 52 / Attribute 51 / Rule 17 / SKU 12 / Category 10 / Policy 9 / Product 8
```

---

## 三、测试内容与命令（按顺序）

### 测试 1：知识引擎单元测试

```bash
NO_PROXY=127.0.0.1,localhost no_proxy=127.0.0.1,localhost \
ALL_PROXY=http://127.0.0.1:9 HTTP_PROXY=http://127.0.0.1:9 HTTPS_PROXY=http://127.0.0.1:9 \
.venv/Scripts/python.exe -m pytest \
  tests/test_knowledge_engine.py \
  tests/test_neo4j_client.py \
  tests/test_graph_retrieval.py \
  tests/test_prompt_templates.py \
  tests/test_graph_api.py \
  tests/test_evaluation_suite.py -q
# 期望: 48 passed + 1 skipped
```

### 测试 2：图谱检索 API（手工验证）

```bash
# 启动服务（用测试配置）
.venv/Scripts/python.exe -c "
from ecommerce_agent.api import create_app
import uvicorn
uvicorn.run(create_app(), host='127.0.0.1', port=8000)"
```

然后访问（用 admin 鉴权）：
```bash
# 实体查询
curl http://127.0.0.1:8000/v1/graph/entity/SKU/sku_id/QC-AF5-WHITE \
  -H "X-Admin-Id: admin-test" -H "X-Admin-Key: test-admin-key-123456"
# 期望: 返回商品属性

# 多跳推理
curl -X POST http://127.0.0.1:8000/v1/graph/multi-hop \
  -H "Content-Type: application/json" \
  -H "X-Admin-Id: admin-test" -H "X-Admin-Key: test-admin-key-123456" \
  -d '{"start_id":"QC-AF5-WHITE","rel_types":["BELONGS_TO","APPLIES_TO-"]}'
# 期望: 返回发货时效/价保/七天无理由 3 条政策

# 关键词检索
curl "http://127.0.0.1:8000/v1/graph/search?q=保修" \
  -H "X-Admin-Id: admin-test" -H "X-Admin-Key: test-admin-key-123456"
# 期望: 返回保修相关政策

# 图谱统计
curl http://127.0.0.1:8000/v1/graph/stats \
  -H "X-Admin-Id: admin-test" -H "X-Admin-Key: test-admin-key-123456"
# 期望: 222 节点 / 241 关系
```

### 测试 3：检索质量评测（35 题）

```bash
.venv/Scripts/python.exe -c "
from ecommerce_agent.knowledge_engine import Neo4jClient, GraphRetrievalService, run_evaluation
svc = GraphRetrievalService(Neo4jClient())
report = run_evaluation(svc, verbose=True)
print(f'通过率: {report[\"pass_rate\"]*100:.0f}%')"
# 期望: 35/35 = 100% 通过
```

### 测试 4：系统质量验证（16 项）

```bash
.venv/Scripts/python.exe -m ecommerce_agent.knowledge_engine.verify_system
# 期望: 16/16 项通过
```

### 测试 5：核心指标抽检

```bash
# 覆盖率（Neo4j 运行中）: 真值表 39 条比对图谱
# 准确率: 抽样计划 60 条比对图谱
# 方法见 docs/gbrain融合方案_云湃知识库.md 的验收指标部分
# 期望: 覆盖率 100% / 准确率 100%
```

---

## 四、测试人需要知道的

### 1. Neo4j 数据需重建
- 数据在 Neo4j 里，不在 git。测试前必须跑"第二节步骤 3"重建
- 密码：neo4j / ${NEO4J_PASSWORD:-change-me}（本地开发用）

### 2. API 鉴权
- 所有 `/v1/graph/*` 端点需要 admin 鉴权
- 头：`X-Admin-Id: admin-test` + `X-Admin-Key: test-admin-key-123456`

### 3. 测试数据是虚拟种子
- 8 个 SPU / 12 个 SKU 是虚拟数据（非真实电商）
- 覆盖率/准确率对虚拟数据达标，不构成生产结论（符合项目边界声明）

### 4. 已知遗留
- 商品数据为虚拟（需授权数据源才真实化）
- 合并记忆需知识满 24h 才触发（当前数据全为新增）

---

## 五、交接物清单

| 交接物 | 位置 |
|---|---|
| 图谱检索服务代码 | `src/ecommerce_agent/knowledge_engine/` |
| API 注册 | `src/ecommerce_agent/api.py` |
| 测试文件 | `tests/test_*knowledge*.py` 等 8 个 |
| Neo4j 导入脚本 | `knowledge_graph_output/04_import/` |
| 评测报告 | `knowledge_graph_output/06_report/retrieval_evaluation_report.json` |
| 交付报告 | `knowledge_graph_output/M3_知识库与RAG检索链路交付报告.md` |
| 方案 + 指标 | `docs/gbrain融合方案_云湃知识库.md` |
