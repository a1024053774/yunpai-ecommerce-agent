# M9-R 第 4 轮修复汇报 — 请 WP5 独立复验

**PR**：#19（`feature/m9r-read-model`）
**Head**：`1d53871`
**Base**：`origin/main` `454b35c9000ab279ffdbf115f80afdf3e031ee73`（merge-base，非 `8de48c3`）
**状态**：OPEN / MERGEABLE，已推送

---

## 一、本轮修复概述

针对第 3 轮复验的 6 个阻断项，按归因归纳的 **5 个根因模式**逐一修复（不逐条打补丁）：

| 阻断项 | 根因模式 | 修复内容 |
|---|---|---|
| 重叠 revision 窗口击穿 item 隔离 | P1 数据身份断裂 | `inventory_balances`/`commerce_orders` 补 `item_id` 列 + `(item_id=? OR item_id IS NULL)` 聚合过滤，重叠窗口跨 item 不再串数 |
| freshness=None 跳过检查返回 True | P2 fail-open 反向防御 | `conclusion_allowed()` 反转：缺失即拒绝（fail-closed） |
| 组合门禁结果只进响应不进决策 | P3 接线只做一半 | `diagnose()` 消费 `run_gates` 的组合结论（gate 是闸门不是装饰品） |
| 生产语义链缺失 | P3 接线只做一半 | 新增 `generate_and_persist_recommendation()` 生产编排 + `POST .../recommendation/generate` 路由 |
| Eval 假覆盖 + 页面缺操作契约 | P4 假覆盖 | `_build_facts_snapshot` 覆盖全 9 类建议（缺信号显式降级）；页面补"生成建议"显式操作 |
| 全量回归 / 跨平台不可复现 | P5 共享配置 | 虚拟店补 D21 `product_lifecycle` 场景；basetemp 移出 addopts；浏览器跨平台显式 skip |

## 二、额外发现并修复

回归归因过程中，发现 **`7de7bef`（httpx 惰性化）引入 2 个测试回归**（非既有失败，Base `454b35c` 上均通过）：
- `test_intent_routing` / `test_chat_stream` 直接访问 `_client.post`，惰性化后 `_client` 为 None → 已改为先 `_ensure_client()` 再 patch

## 三、独立验收证据（对齐验收手册）

- **编译门禁**：`compileall` EXIT=0；`pytest --collect-only` 1266 tests 无收集错误
- **独立反例探针**：12 passed（item 隔离 4 + freshness 4 + 生产链 4，独立 seed 不复用提交内 oracle）
- **mutation 反证**：2 组红绿循环（freshness 反转→红→还原→绿；item 过滤移除→红→还原→绿）
- **生产调用链 grep**：`engine.generate` 唯一生产调用点在 `business/service.py`（生产编排），被 POST 路由消费
- **4 个 WP 验收脚本**：WP1-WP4 全部 EXIT=0（FAIL 时 `sys.exit(1)` 防假绿）
- **M9-R 定向测试**：239 passed
- **浏览器门禁**：真实 uvicorn + Playwright + console 监听 + 溢出检查 + 操作契约（点击生成建议→DRAFT 落库→审计）。**注明**：浏览器门禁为本机 Windows Edge 实跑证据；复验环境无 Edge/Playwright 时该测试显式 skip（非失败）。
- **全量回归**：1264 passed / 1 failed——唯一失败为 `7de7bef` 惰性化回归，已修复并单测验证（修复后未重跑全量，如需最终全绿证据可补跑 ~20 分钟）

**验收文档**：
- [验收报告](docs/reviews/M9R-WP5-ACCEPTANCE-REPORT.md)
- [验收协议](docs/reviews/m9r-wp5-acceptance-protocol.md)
- [WP 验收矩阵](docs/reviews/M9R-WP5-matrix.md)
- [独立探针输出](docs/reviews/M9R-WP5-probes-output.txt)

## 四、签署边界（诚实声明）

**已覆盖**：编译/收集门禁、独立反例探针、mutation 反证、生产调用链 grep、防假绿、跨平台收集、浏览器操作契约、回归归因。

**未覆盖（不冒充完成）**：
- 真实模型 E2E——需 API key 环境；D-034 达标已用 mock ModelGateway 证明 `gateway.calls==1`
- 真实平台接入（淘宝/ERP/数据库）——PR 为只读数据基础设施，不连接外部
- 业务签署、生产放行

## 五、复验复现命令

```bash
git worktree add --detach D:/m9r-verify 1d53871 && cd D:/m9r-verify
python -m compileall -q src tests
PYTHONPATH=src python -m pytest --collect-only -q
PYTHONPATH=src python -m pytest -q \
  tests/test_m9r_item_isolation_overlap.py \
  tests/test_m9r_diagnosis_freshness_none.py \
  tests/test_m9r_production_recommendation_chain.py
PYTHONPATH=src python tests/verify_wp{1,2,3,4}_acceptance.py
PYTHONPATH=src python -m pytest tests/test_m9r_workbench_browser.py -q
PYTHONPATH=src python -m pytest tests -q   # 全量回归（单进程串行 + D 盘 basetemp）
```

**请从干净状态独立复验，结论按验收手册出（通过 / 不通过 + 阻断项）。**
