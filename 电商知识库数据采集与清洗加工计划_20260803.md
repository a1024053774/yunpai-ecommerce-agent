# 电商知识库数据采集与清洗加工计划

> **版本** v2.1 · **日期** 2026-08-03 · 交付模块前半段：采集 → 清洗 → 实体抽取 → 关系构建 → Neo4j 导入 → 抽检

---

## 阅读速览（30 秒掌握全貌）

**一句话定位**：从种子数据 + 项目知识源 + 公开规则三类源数据，一体化产出可直接导入 Neo4j 的知识图谱，并给出覆盖率 / 准确率抽检报告。

**关键数字，先记这四个：**

| 指标 | 数字 | 达标线 |
|---|---|---|
| 核心实体覆盖率 | 分母 **40**（SPU 8 + SKU 12 + 品类 10 + 政策 ≈10） | ≥ **36/40**（90%） |
| 关系准确率 | 核心池抽 **60 条** | 总体 ≥ **51**（85%）**且** 每类 ≥80% |
| 本体口径 | 六类实体 / 五类关系 | Q7 验证全覆盖 |
| 交付物 | ①Schema ②数据集 ③图谱实例 ⑦抽检报告 | §12 清单 |

**v2.1 相对 v2 改了什么**（详见正文各节）：
- **验收可信度**：分母锁死、双门槛、95% 置信区间、整改→复检闭环（§6.3 / §7）
- **实体模型**：属性分级、政策聚合、FAQ 数量拆解、品类层级锁死（§2 / §4）
- **执行风险**：网络抓取降级链、M0 环境自检、LLM 降级路径、三方角色分离、工时估算（§3 / §10）

---
d'd'wa'da
## 0. 任务定位

本任务属于"电商知识库（Wiki + 知识图谱双引擎）"模块的前半段，只覆盖六步中的 **①②③ + 抽检**：

| 步骤 | 内容 | 本计划 |
|---|---|---|
| ① | 本体 Schema 设计（六类实体 / 五类关系 / 属性） | ✅ §2 + 交付 ontology_schema.md |
| ② | 源数据采集 → 结构化 → 实体抽取 + 关系构建 | ✅ §3~6（核心） |
| ③ | Neo4j 部署与导入 | ✅ §8 |
| ④⑤⑥ | Wiki 系统 / 检索 API / 四套 Prompt + 评测 | ⏭ 后续任务，Schema 契约已对齐（§2.4） |

**上游输入（决策1=B）**：无既有知识文档作为上游。六类/五类是**最终交付口径**，从源数据一体化构建。

**执行现状（2026-08-03 实测）**：本计划已完整执行至 M2 末（采集→清洗→抽取→关系构建→导出），产出落盘 `knowledge_graph_output/`。**六类实体 194**（品类 10 + SPU 8 + SKU 12 + 属性 51 + 政策 9 + 话术 52 + FAQ 60）+ **五类关系 213**（属于 12 + 具有 51 + 适用 34 + 引用 64 + 关联 52），校验 0 错误，CSV/Cypher 导入文件 + dictionary_schema.json 契约已就绪（§8.1/§2.4）。M3（Neo4j 导入验证）与 M4（人工抽检）待执行。**实测两处修正**：① knowledge_seed 实际 **52 条**（计划原写 43）；② 核心实体分母按实际政策 9 计 = **39**（SPU 8 + SKU 12 + 品类 10 + 政策 9），达标线 90% = ≥36/39。

> **📋 任务6执行记录（2026-08-04）**：任务6（采集与清洗加工）已闭环交付，验收 2/2 通过。完整交付报告见 `knowledge_graph_output/任务6交付报告_知识库数据采集与清洗加工.md`（含结果摘要/完成内容/证据清单/验收结果/风险与下一步五段），交接包 `knowledge_graph_output/07_handoff/`（含压缩包 `07_handoff.zip`）。任务4/2 承接输入已预准备（六类实体 + 五类关系 + 真值表 39 + 抽样计划 60 + CSV/Cypher）。

**双引擎框架策略（本次交付定位）**：Wiki 与知识图谱**都做，但只搭框架**——骨架、契约、管线、达标机制做扎实；内容深浅与功能细节留待具体需求到达后再细化，不提前做死。

| 做扎实（框架） | 留弹性（细节后置） |
|---|---|
| dictionary_schema.json 契约、六类五类骨架 | 更多品类/数据源扩充、LLM 深度辅助 |
| 采集 → 抽取 → 导入 → 验证全链路可跑通 | 复杂关系精细判定、图谱性能调优 |
| 双指标达标机制（覆盖率/准确率） | Wiki 界面/权限/插件 |

同一份数据（`02_clean/`）同时供**图谱导入**与 **Wiki 文档导入**；图谱承担校验/溯源/确定性查询，Wiki 承担检索/编辑/LLM 底座。**对任务书的归属**：本计划横跨 任务6（§3–5 采集清洗）+ 任务4（§6–7 抽取构建）+ 任务2（§8 图谱导入），§12 交接清单即各任务边界。

---

## 1. 已确认决策

| 编号 | 决策 |
|---|---|
| D1 | 一体化方案：无上游知识文档，从源数据采集 + 清洗 + 抽取 + 构建 |
| D2 | 本体口径 = 六类实体 / 五类关系 |
| D3 | 覆盖率与准确率用**真值表比对**验证 |
| D4 | 数据来源 = 项目内种子/知识源为主 + 公开权威规则为辅 |
| D5 | 品类 = 小家电（5 种）+ 数码 + 服饰 |
| D6 | 核心实体 = SPU + SKU + 品类 + 售后政策；**覆盖率分母锁死 40** |
| D7 | 验收只针对五类核心关系（抽 60 条）；扩展关系独立抽 20 条，**不参与达标** |
| D8 | rule-based 关系置信度 = 1.0；LLM 判定 = 自评 × 一致性系数；< 0.8 进复核 |

**规模预估（参考值，非验收口径）**：节点 ≈200–220（商品 20 + 品类 10 + 属性 ≈25 + 政策 ≈10 + 话术 43 + FAQ ≈55 + 扩展 ≈30）；关系 ≈300–380（五类核心 ≈280–340 + 扩展 ≈28）。**以 §7 抽检报告与 graph_stats.json 实测为准；真值表分母 40 不随预估浮动。**

---

## 2. 本体 Schema

### 2.1 六类实体

#### ① 商品 Product —— SPU + SKU 两层
- 唯一键：`item_id`（SPU）/ `sku_id`（SKU）
- 关键属性：title、model、status(active/inactive/retired)、sale_price(CNY)、warranty_months、gtin、source(fixture/network/manual)
- 数量：种子 5 SPU / 6 SKU；新增品类 3 SPU / 6 SKU

#### ② 品类 Category —— 多级树，层级锁死
- 唯一键：`category_code`；关键属性：category_name、parent_category、level
- **数量锁死 = 10**（顶层 3 + 二级 7），完整实例见 §4.4

#### ③ 属性 Attribute —— 分两级，避免重复节点
- **SPU 级**（品牌/型号/材质，全 SKU 共享）：`spec_key = {item_id}|{attr_key}`，例 `QC-SPU-AF5|brand = 晴川`
- **SKU 级**（颜色/容量/尺码，规格差异）：`spec_key = {sku_id}|{attr_key}`，例 `QC-AF5-WHITE|color = 白`
- **为什么分级**：brand/model 在种子 catalog 的 item（SPU）层。若全挂 SKU，6 个 SKU 会重复生成 6 个"晴川"节点。

#### ④ 售后政策 Policy —— 按内容聚合
- 唯一键：`policy_code = {PREFIX}-{hash8}`（PREFIX ∈ WARRANTY/RETURN/REFUND/EXCHANGE/LOGISTICS/INVOICE/PRICE）
- 关键属性：policy_type、content、scope(Category/Product/SKU)、risk_level(low/medium/high/critical)、effective_from/to、source
- **聚合规则**：同 policy_type + 同内容 + 同范围 → 只建 **1 条**政策节点 + 多条 APPLIES_TO 边。例：6 个 SKU 同是"保修 12 个月" → 1 条 `WARR-{hash8}`，不是 6 条雷同政策。

#### ⑤ 客服话术 Script —— 直接映射
- 唯一键：`script_id`
- 关键属性：category、intent、canonical_answer、layer(platform/industry/store/product/evolution)、risk_level、aliases
- 来源：knowledge_seed.py 的 43 条 TOPICS 直接映射（已验证，含约 156 条问法）

#### ⑥ FAQ —— answer 可派生，不冗余
- 唯一键：`faq_id`；关键属性：category、intent、question、canonical_answer、risk_level、layer、aliases、ref_script_id
- **answer 派生**：有 `ref_script_id` 时引用对应 Script 的 canonical_answer，导出时物化，不双份存储
- **数量构成**：43（Script 提炼）+ 4（种子 knowledge）+ 6（3 新品类 × 2）+ N（网络素材）≈ **55–65，基线 55**

### 2.2 五类关系

| 关系 | 标签 | 方向 | 示例 |
|---|---|---|---|
| 属于 | BELONGS_TO | 商品 → 品类 | SKU → air_fryer |
| 具有 | HAS_ATTR | 商品/SKU → 属性 | SKU → color=白 |
| 适用 | APPLIES_TO | 政策 → 品类/商品/SKU | Policy → air_fryer |
| 引用 | REFERS_TO | FAQ → 商品/品类/政策/话术 | FAQ"保修多久" → Policy |
| 关联 | RELATED_TO | 话术↔FAQ、政策↔规则 等 | Script → FAQ |

> **抽样池口径（D7）**：上面五类是**核心关系池**（验收抽检对象）。扩展支撑关系（CONTAINS / STOCKED_AT / CARRIED_BY / HAS_AFTERSALE / OPERATES_ON）全部入图，但只入扩展池，**不参与达标判定**。

### 2.3 实体 × 关系矩阵（六类 × 五类 + 扩展）

| | 属于 | 具有 | 适用 | 引用 | 关联 |
|---|---|---|---|---|---|
| 商品 | →品类 | →属性 | 被适用 | 被引用 | →品牌（扩展） |
| 品类 | →父品类 | — | 接收适用 | 被引用 | — |
| 属性 | — | — | — | — | — |
| 售后政策 | — | — | →品类/商品 | 被引用 | →规则（扩展） |
| 客服话术 | — | — | — | — | →FAQ |
| FAQ | — | — | — | →商品/品类/政策/话术 | — |

### 2.4 Schema 契约（供下游 Wiki / 检索 API / Prompt 对齐）

- ID 命名 `{前缀}-{业务键}`；价格 decimal(10,2)+CNY；日期 ISO8601+UTC；文本全半角统一
- 枚举字典（status / policy_type / layer / risk_level / attr_key…）固化在 `03_dictionary/data_dictionary.md`
- 节点标签、关系类型、属性名在 `dictionary_schema.json` 中固化为**唯一契约**——下游检索 API 字段名、防幻觉 Prompt 注入字段、Wiki 分类全部取自本契约

---

## 3. 数据源（从哪里获取）

### L0 本地种子
| 数据源 | 内容 | 进图归属 |
|---|---|---|
| `fixtures/virtual_store_v1.json` | 晴川家电旗舰店：6 SKU、10 库存、8 订单、3 售后单、4 FAQ、5 物流商、2 仓库 | 商品/品类/属性/FAQ + 订单库存扩展节点 |

### L1 项目知识源
| 数据源 | 内容 | 进图归属 |
|---|---|---|
| `knowledge_seed.py` | 43 条 SOP（category/intent/answer/questions） | 43 条 Script + FAQ 提炼 |
| `rag.py` | knowledge 字段契约（id/category/intent/.../sku_id） | 字段对齐依据 |
| `business/inventory.py` | 断货口径 `available = on_hand - reserved` | 运营验证场景 Q4 |

### L2 公开权威源（标注来源 URL + 抓取日期）
- **S1** 七天无理由退货 · 淘宝规则中心 · → Rule + Policy(return)
  `jianghu.taobao.com/detail/47301_58660388` ✅已核
- **S2** 国家三包规定 · 市场监管总局 · → Rule(industry) + Policy(warranty)
  `samr.gov.cn/zw/zfxxgk/zc/xzgfxwj/art/2019/art_f943e66de125437d92df701e1654c47c.html` ✅已核
- **S3** 消费者权益保护法 · 全国 12315 · → Rule(platform)
  `www.12315.cn/knowledge/knowledgeView?zlcode=956831a206d548d8a72934a592414e4b` ✅已核
- **S4** 价格保护（7 日/活动限制）· → Rule + Policy(price_protection) ⏳
- **S5** 发货时效与延迟赔付 · → Rule ⏳
- **S6** 3C 认证（数码合规）· → Rule ⏳
- **S7** 小家电售后细则（保修/滤网 H13 周期）· → Policy + FAQ ⏳
- **S8** 数码品类规则（电池保修/激活后退货）· → Rule + FAQ ⏳
- **S9** 服饰品类规则（吊牌/尺码/内衣特殊）· → Rule + FAQ ⏳
- **S10** 客服话术范本（物流异常/发票/拒收/换货）· → FAQ + Script ⏳

⏳ 项执行阶段用 WebSearch 定位官方页面再抓取（规则页 URL 易变动，留快照）。

**抓取降级链**（S1–S10 通用，落到哪级都记录 `来源URL | 抓取日期 | 主题 | 进图归属`）：
1. WebFetch 抓静态正文（首选）
2. WebSearch 定位并取结果快照
3. 人工从浏览器复制正文（URL 失效则检索官方转载/存档，仍标原始来源）

**合规**：只摘录事实性规则、法条、平台公开服务规范，按"事实性引用 + 来源署名"处理；不采集用户隐私、不做反爬对抗。

### L3 人工整理
- 新增品类（数码/服饰）规格、保修口径，按 seed 的 catalog 字段契约补全
- 覆盖矩阵校验：四大类知识 × 三层来源，无空白

---

## 4. 数据采集

### 4.1 种子提取（01_extract.py）
- `catalog`：按 item_id 分组 → 5 Product + 6 SKU；attributes 的 brand/model → SPU 级属性，color 等规格差异键 → SKU 级属性；warranty_months → 进 Policy 聚合输入（§2.1④）
- `inventory` 10 行 → 2 Warehouse + 10 条 STOCKED_AT（on_hand/reserved/inbound/avg_daily_sales/as_of）
- `orders` 8 笔 → 8 Order + 8 CONTAINS + 5 Carrier + 6 CARRIED_BY（logistics=null 的 2 笔不建）+ 3 AfterSaleCase + 3 HAS_AFTERSALE
- `knowledge` 4 条 → 并入 FAQ 池（不单独叠加计数）

### 4.2 知识源抽取
- 43 条 TOPICS → 43 条 Script；每条取 canonical 问法提炼 1 条 FAQ，其余问法入 `aliases`；Script→FAQ 建 RELATED_TO
- FAQ 数量构成与 answer 派生规则见 §2.1⑥

### 4.3 品类层级（锁死 = 10）

| category_code | 名称 | 父级 | 挂接 SKU |
|---|---|---|---|
| home_appliance | 小家电 | — | — |
| air_fryer / cordless_vacuum / humidifier / electric_kettle / air_circulation_fan | 空气炸锅/无线吸尘器/加湿器/电热水壶/循环风扇 | home_appliance | QC-AF5 两款 / QC-VC-A1 / QC-HD-A1 / QC-KT-A1 / QC-FA-A1 |
| digital | 数码 | — | — |
| digital_audio / digital_power | 数码音频/数码电源 | digital | X-M1 两款 / X-P2 |
| apparel | 服饰 | — | Y-D01 三款 |

> 顶层 3 + 二级 7 = **10**，直接进真值表分母。执行期若调整层级，必须同步更新分母并记录变更。

**新增品类**：
- **数码**（品牌星脉）：蓝牙耳机 X-M1（白/黑 2 SKU）、充电宝 X-P2（1 SKU）；属性 color/storage_gb/battery_mah（SKU 级）+ 品牌/型号（SPU 级）；电池保修走 3C + 厂商政策
- **服饰**（品牌云织）：羽绒服 Y-D01（S/M/L 3 SKU）；warranty_months=0 走三包退换；属性 size/color/material/season
- 每个新品类配套 ≥1 条 Policy、≥2 条 FAQ（对齐 Q6）

---

## 5. 清洗与校验

**去重**：
- SKU 按 sku_id 唯一；FAQ 按 (intent, question_normalized) 合并，多问法入 aliases
- Policy/Rule 按 (code, scope_key, content_hash) 聚合；Script 按 (intent, canonical_answer_hash)；Carrier 按 carrier_name

**校验（03_validate.py，产出 validation_report）**：
- 必填缺失拒绝；外键一致（SKU.item_id ∈ Product、FAQ.ref_script_id ∈ Script、关系端点存在）
- 取值合法（price/on_hand/quantity ≥ 0，effective_from ≤ effective_to）；枚举合法
- 安全：无明文手机/地址（hash/脱敏）、不承诺退款金额绝对话术

---

## 6. 实体抽取与关系构建

### 6.1 抽取策略

| 输入 | 目标实体 | 方法 |
|---|---|---|
| 商品信息 | Product/SKU + 属性 | **规则**（字段契约映射） |
| 售后政策（S1–S10 + 种子） | Policy | **规则 + LLM**，人工核对 |
| 客服话术（43 条 + S10） | Script + FAQ | **规则**（TOPICS 结构映射） |
| 行业规则（S2/S3/S6 法条） | Rule（扩展） | **规则 + LLM** |

- 每条记录带 `source`（fixture/network/manual）+ `extracted_by`（rule/llm+human/llm）
- **LLM 降级路径**：仅用于非结构化文本辅助提炼；环境无 LLM 时**自动降级为纯规则 + 人工标注**，不阻塞主流程。是否用 LLM、哪家模型、成本上限在 M1 开始确认并登记 `llm_usage.md`

### 6.2 关系构建与置信度

| 关系 | 判定规则 | 置信度（D8） |
|---|---|---|
| BELONGS_TO | category 字段映射；品类→父品类 | rule-based = **1.0**，不进复核 |
| HAS_ATTR | 剩余 attributes 键 → 属性（两级） | rule-based = **1.0**，不进复核 |
| APPLIES_TO | Policy.scope 声明 + 条款与品类匹配 | LLM 判定 |
| REFERS_TO | FAQ 的 intent/sku_id/ref_script_id → 目标 | LLM 判定 |
| RELATED_TO | Script→FAQ、Policy→Rule、FAQ→FAQ | 声明 + 相似度 |

- **LLM 判定公式**：`confidence = 自评分数 × 一致性系数`（≥2 次独立抽取结果一致 = 1.0，不一致 = 0.5 转人工裁决）
- **唯一入口**：`confidence < 0.8` → 人工复核队列，复核通过才计入关系池

### 6.3 真值表（覆盖率基准，分母锁死）

**分母 = 40**，构成与达标线：

| 核心实体 | 数量 | 说明 |
|---|---|---|
| 商品 SPU | 8 | 种子 5 + 数码 2 + 服饰 1 |
| 商品 SKU | 12 | 种子 6 + 数码 3 + 服饰 3 |
| 品类 | 10 | §4.3 锁死 |
| 售后政策 | ≈10 | 种子聚合 + 网络提取 + 新品类配套（唯一允许浮动的分子） |
| **合计** | **40** | **≥ 36/40 = 90%** |

- SKU 命中口径：`sku_id` + 关键 SKU 级属性 + 归属 SPU 全对
- **标注角色独立**：真值表由独立标注员标注，**不得与实体抽取执行人同一人**（角色分工见 §7.5）
- 落盘 `06_report/truth_table.csv`，字段 `doc_id | entity_key | entity_type | relation_key | relation_type | expected_value`

---

## 7. 覆盖率与准确率抽检（核心交付）

### 7.1 指标定义

**覆盖率** = 抽取结果与真值表匹配数 ÷ 40（匹配 = 唯一键一致 且 关键属性一致），**≥ 36/40**。

**准确率（双门槛）** = 人工判定的正确关系数 ÷ 抽检数（正确 = 头尾实体 + 关系类型 + 方向三者全对）：
- 门槛 1：总体 **≥ 51/60（85%）**
- 门槛 2：五类**每类 ≥ 80%**，防某一类崩坏被总量掩盖
- 报告附 **95% 置信区间**，不只报点估计

### 7.2 抽样设计（核心池 60 + 扩展池 20）

| 关系 | 生成机制 | 抽样条数 | 每类达标线 |
|---|---|---|---|
| BELONGS_TO | rule-based | 10 | ≥ 8 |
| HAS_ATTR | rule-based | 10 | ≥ 8 |
| APPLIES_TO | LLM 判定 | 14 | ≥ 12 |
| REFERS_TO | LLM 判定 | 14 | ≥ 12 |
| RELATED_TO | 声明+相似度 | 12 | ≥ 10 |
| **合计** | | **60** | 总体 ≥ 51 |

- **风险加权**：LLM 判定类多抽（14/14/12），rule-based 少抽（10/10）
- 扩展关系独立抽 **20 条**，内部质量把关，**不参与达标**
- 随机种子固定（`random.Random(20260803)`），可复现

### 7.3 流程与整改闭环

流程：真值表 → 覆盖率全量比对（40 条）→ 分层随机抽样 → 双人交叉复核（分歧三人合议）→ 汇总 → 报告。

**整改→复检**：任一门槛不过 → 出具整改清单（错误关系 + 原因归类 + 修正动作）→ 错误样本全量重判 + 重抽 60 复测 → **循环至双门槛全过**才进 M5。每轮留档 `sampling_result.csv`（含 round 字段）。

### 7.4 抽检报告（10_report.py 产出）

- 覆盖率：40 条逐条命中/未命中明细 + 原因分类
- 准确率：分层抽样明细 + 判定结果 + 错误归类 + 95% CI
- 达标判定 + 整改轮记录 + 遗留问题清单

### 7.5 角色分工（防自证）

| 角色 | 职责 |
|---|---|
| 数据源采集员 | 抓取/落盘 S1–S10 |
| **真值表标注员** | 标注核心实体 + 关系真值基准 |
| 实体抽取执行人 | 规则 + LLM 抽取、关系构建 |
| **抽检复核员（双人）** | 逐条判定抽样关系，分歧三人合议 |
| 项目负责人 | 终审达标判定、关闭整改项 |

**铁律：标注员 ≠ 抽取人 ≠ 复核员，三方交叉。**

---

## 8. 输出与 Neo4j 导入

### 8.1 输出结构
- `04_import/`：nodes_*.csv + rels_*.csv（每类型一个）+ `00_setup.cypher`（唯一约束 ×15）+ `01_load_nodes.cypher`（MERGE+SET）+ `02_load_rels.cypher`
- JSON 备份：`02_clean/*.json`（与 CSV 同源，供 Wiki 复用）
- 选型：官方 `neo4j` 驱动（不用 py2neo）；LOAD CSV + MERGE（幂等可重跑）

**增量导入机制（任务2 硬性要求）**：
- 所有节点/关系 CSV 带 **`updated_at` 字段**（ISO8601）
- **新增知识** → 重跑导入，MERGE 按唯一键自动插入新节点/关系，幂等
- **更新知识** → MERGE 命中后 SET 覆盖属性并刷新 `updated_at`，**不重建索引/约束**（约束一次性在 `00_setup.cypher` 创建）
- **全量重导** → 仅删 `MATCH (n) DETACH DELETE n` 后重跑，幂等安全
- 支持增量即无需"重建全部索引"，天然满足任务书要求

### 8.2 导入预检（8 项）

**导入前（脚本自动）**：
1. CSV 列名 ⊆ Schema 属性集
2. 唯一键非空且全局唯一
3. 关系起/终点 ID 均能在节点 CSV 找到
4. 枚举/数值/日期类型匹配
5. UTF-8 无 BOM、列数与表头一致

**导入后（08_verify.py 查图）**：
6. 无孤儿节点（无任何关系的节点 = 0）
7. 六类实体、五类关系各 ≥ 指定最小样本
8. Q1–Q7 验证查询全部返回预期

### 8.3 验证查询（覆盖五类关系）
- Q1 事实链路：air_fryer → Product → SKU → Policy(warranty) 返回 AF5 保修 12 个月
- Q1b 话术链路：FAQ REFERS_TO QC-SPU-AF5 返回保修话术
- Q2 运营：在售 SKU 按品类统计，合计 = 12
- Q3 适用：Policy APPLIES_TO air_fryer
- Q4 断货：STOCKED_AT 上 (on_hand-reserved) ≤ 0 命中 QC-VC-A1
- Q5 溯源：Policy → Rule → Platform（关联链）
- Q6 新品类：digital_audio / digital_power / apparel 各 ≥ 2 条 FAQ
- Q7 类型覆盖：五类关系各 1 条代表性 MATCH 返回非空

---

## 9. D 盘目录结构

```
D:\ecommerce-knowledge-graph\
├─ README.md
├─ 01_raw\          seed\ + knowledge\ + network\S1..S10.md
├─ 02_clean\        六类实体 + 扩展节点 JSON + clean_manifest.json
├─ 03_dictionary\   ontology_schema.md + data_dictionary.md + dictionary_schema.json
├─ 04_import\       nodes_*.csv / rels_*.csv / 00_setup / 01_load_nodes / 02_load_rels
├─ 05_scripts\
│  00_env_check.bat 01_extract.py 02_network_capture.py 03_normalize.py 04_validate.py
│  05_export.py 06_neo4j_setup.bat 07_neo4j_import.py 08_verify.py 09_sampling.py 10_report.py
├─ 06_report\       env_check / validation_report / truth_table / sampling_result /
│                   coverage_accuracy_report / graph_stats / llm_usage
└─ 07_handoff\      交接包汇总
```

CSV 拷入 `D:\neo4j-community-2026.04.0\import\kg\`，LOAD CSV 用 `file:///kg/...`。

---

## 10. 里程碑与工时

| 阶段 | 动作 | 关口 | 工时 |
|---|---|---|---|
| **M0 环境自检** | ① neo4j 版本 ② Java/JAVA_HOME(17+) ③ auth 文件→决定设密方式 ④ 端口 7474/7687 ⑤ import/kg 目录 ⑥ start/status | 6 项全过才进 M1 | 0.5 人日 |
| M1 采集 | 提取种子 + 知识源；S1–S10 降级链落盘 | 四大类知识覆盖无空白 | 1.5–2 人日 |
| M2 清洗抽取 | 标准化/去重/校验/抽取/关系构建/导出 | 校验 0 错误；预检 8 项通过 | 2–3 人日 |
| M3 导入 | 设密→约束→节点→关系→验证查询 | Q1–Q7 全过；无孤儿 | 1 人日 |
| M4 抽检 | 真值表→覆盖率→抽样复核→达标判定→整改循环 | 覆盖率 ≥90% 且双门槛过 | 2–3 人日 |
| M5 交接 | 07_handoff 打包 | 交接清单齐全 | 0.5 人日 |
| **合计** | | | **7.5–10 人日** |

**主要风险对策**：Neo4j 首次认证 → 前置到 M0③；中文 CSV → UTF-8 无 BOM；Cypher 5.x 兼容 → M0①锁定版本；S4–S10 URL 易变动 → 降级链 + 留快照；LLM 不可用 → 自动降级纯规则；真值表/抽检三方分离 → §7.5。

---

## 11. 验收标准对照

- **实体抽取覆盖六类** → §2.1/§6.1，Q7 验证
- **关系构建覆盖五类** → §2.2/§6.2，Q3/Q7 验证
- **核心实体覆盖率 ≥90%** → 分母 40、≥36/40 全量比对（§6.3/§7.1）
- **关系准确率 ≥85% 人工抽检** → 核心池 60 条、双门槛（§7.1/§7.2）
- **输出含全部实体/关系类型样本** → §8.1 + 预检第 7 项
- **Neo4j 预检通过** → §8.2 八项
- **图数据库部署完成** → §8 + Q1–Q7

---

## 12. 交接清单

| # | 交接物 | 位置 |
|---|---|---|
| ① | 本体 Schema 文档 | `03_dictionary/ontology_schema.md` + `dictionary_schema.json` |
| ② | 结构化数据集（CSV + JSON + Cypher） | `02_clean/` + `04_import/` |
| ③ | 知识图谱实例 | Neo4j + `07_handoff` 导出快照 |
| ⑦ | 覆盖率/准确率抽检报告 | `06_report/coverage_accuracy_report.md` + truth_table + sampling_result |
| 衔接 | Wiki / 检索 API / 四套 Prompt / 评测 | 后续任务，契约已对齐（§2.4） |

---

## 附录：v2.1 修订对照（相对 v2）

| 编号 | 问题 | 处理位置 |
|---|---|---|
| A1 | 真值表分母口径矛盾（26–30 vs 明细合计 38） | §6.3 分母锁死 40 |
| A2 | 60 条抽样统计效力弱、缺整改闭环 | §7.1/7.3 双门槛 + CI + 复检循环 |
| A3 | 置信度 <0.8 来源未定义 | §6.2 rule-based=1.0 / LLM=自评×一致性 |
| A4 | 抽样池是否含扩展关系未澄清 | §2.2/7.2 核心池 60 + 扩展池 20 |
| B5 | 属性全挂 SKU 导致重复节点 | §2.1③ 属性分级 |
| B6 | warranty→Policy 缺生成规则 | §2.1④ 聚合 + policy_code |
| B7 | FAQ 数量来源不清、答案冗余 | §2.1⑥ 数量拆解 + answer 派生 |
| B8 | 品类顶层节点未定义 | §4.3 层级锁死 = 10 |
| C9 | requests 抓规则页高估可行性 | §3 降级链 + 合规 |
| C10 | Neo4j 环境未预检 | §10 M0 关口 |
| C11 | LLM 依赖与真值表循环风险 | §6.1 降级 + §7.5 角色分离 |
| C12 | 缺工时估算 | §10 合计 7.5–10 人日 |
